# Pinned runtime dependencies. Add new packages with explicit version
# pins (e.g. `numpy==1.26.4`). Dev-only packages (pytest, black, ruff)
# can go in a separate `requirements-dev.txt` if you prefer to split
# them.

# Core
# pyyaml==

# Data / ML (uncomment what the project actually uses)
# numpy==
# pandas==
# scikit-learn==
# matplotlib==
# jupyter==

# Software engineering (uncomment what the project actually uses)
# click==          # CLI framework
# pydantic==       # data validation / config models
# httpx==          # HTTP client
# fastapi==        # web framework

# Testing
# pytest==

# Domain-specific (fill in based on project needs)
