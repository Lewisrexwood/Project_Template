# {{PROJECT_NAME}}

{{One-sentence tagline describing what this project is.}}

## Setup

```bash
python -m venv .venv
# Windows
.venv\Scripts\activate
# macOS / Linux
source .venv/bin/activate

pip install -r requirements.txt
```

## Tests

```bash
pytest tests/ -v
```

## Docs

Start with [`CLAUDE.md`](CLAUDE.md). Active planning lives in
`docs/superpowers/plans/`.

## Layout

See the "Project Structure" section of [`CLAUDE.md`](CLAUDE.md).
